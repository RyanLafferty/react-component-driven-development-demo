import React from 'react';
import styles from './SCSSModuleExampleComponent.module.scss';

const SCSSModuleExampleComponent = () => (
  <div className={styles.container} />
);

export default SCSSModuleExampleComponent;
